name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show project structure
        run: |
          pwd
          ls -la
          echo "Searching for pom.xml..."
          find . -name pom.xml

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: zulu

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build, test, coverage, and SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          POM_PATH=$(find . -name pom.xml | head -n 1)

          if [ -z "$POM_PATH" ]; then
            echo "ERROR: No pom.xml file found in the repository."
            exit 1
          fi

          PROJECT_DIR=$(dirname "$POM_PATH")

          echo "Found pom.xml at: $POM_PATH"
          echo "Running Maven inside: $PROJECT_DIR"

          cd "$PROJECT_DIR"

          mvn -B clean verify sonar:sonar \
            -Dsonar.projectKey=appointment-2026_SoftwareProject \
            -Dsonar.organization=appointment-2026 \
            -Dsonar.host.url=https://sonarcloud.io
